---
name: steel-lingo-lite
description: 일본어 입문 학습 앱 "스틸링고 Lite"의 새 회차를 만들거나 기존 회차를 고칠 때 쓴다. DATA 배열에 들어가는 객체 형식을 그대로 지켜 원고를 쓰고, 검증 스크립트로 확인한 뒤, 5인 페르소나 3라운드 심사에서 평균 9.8점 이상을 받아야 완성으로 친다. 사용자가 "스틸링고", "스틸링고 라이트", "일본어 초급", "새 회차", "21회차", "단어 추가"를 언급하면 반드시 이 스킬을 쓸 것. 짝이 되는 실무자용 앱은 "스틸링고 Pro"이고 그쪽은 별도 스킬(steel-lingo-pro)이다 — 사용자가 "트레이닝"이나 "Pro"라고 하면 그쪽으로 넘길 것.
---

# 스틸링고 Lite 회차 만들기

곤사마(@POSCO)가 만든 일본어 입문 학습 웹앱. 짝이 되는 실무자용 앱 **스틸링고 Pro**와
표기 규칙은 같지만 데이터 형식과 독자가 완전히 다르므로 스킬도 따로 둔다.

| | 내용 |
|---|---|
| 대상 | 일본어를 막 시작한 사람 |
| 소재 | NHK News Web Easy 수준 생활 뉴스 |
| 현재 | 20회차 |
| 명세 | `references/schema-lite.md` |
| 검증 | `node scripts/check_day.js --lite 파일` |
| 심사 | `references/review-panel.md` — 5인 페르소나 3라운드, 평균 9.8점 이상 |

## 작업 순서

1. **명세를 읽는다** — `references/schema-lite.md`. 여기 요약만 보고 쓰면 반드시 어긋난다.
2. **소재 정하기** — 사용자에게 묻거나 제안한다. 뉴스 한 편으로 소화되는 생활 소재
   (날씨·재해, 생활 정보, 계절 행사 등). NHK News Web Easy에서 실제 기사를 참고해도 좋다.
3. **원고 쓰기** — 문체와 난이도 기준은 `references/style.md`를 읽고 따른다.
4. **검증** — `scripts/check_day.js --lite 파일`을 돌린다. 통과할 때까지 고친다. 건너뛰지 않는다.
5. **심사** — `references/review-panel.md`의 5인 페르소나로 3라운드 평가한다.
   평균 9.8점 미만이면 지적받은 곳을 고치고 다시 심사한다. 이 단계를 생략하지 않는다.
6. **전달** — 완성된 회차 객체를 `.js` 파일로 저장해 사용자에게 보낸다.

## 표기 규칙

앱이 화면에 그릴 때와 음성으로 읽을 때 쓰는 표기가 세 가지다. 이걸 틀리면 후리가나가 깨진다.

| 쓰는 법 | 화면 | 음성 | 쓰는 곳 |
|---|---|---|---|
| `場所[ばしょ]` | 場所에 ばしょ 후리가나 | 한자 그대로 엔진에 넘김 | 모든 일본어 |
| `{9時}[くじ]` | 9時 (후리가나 없음) | くじ | 숫자·시각·단위·연도 |
| `《雨[あめ]》` | 노란 형광펜 | 표시 없이 읽음 | 그 회차 단어가 문장에 나올 때 |

- 한자 덩어리에만 대괄호를 붙인다. 오쿠리가나는 밖에 둔다 — `降[ふ]る`가 맞고 `降る[ふる]`는 틀리다.
- 히라가나·가타카나만 있는 말에는 대괄호를 붙이지 않는다 — `やむ`, `ください`.
- **숫자와 단위는 반드시 `{}[]`로 읽기를 달아라.** 음성 엔진은 「9時」를 「きゅうじ」로,
  「2026年度」를 엉뚱하게 읽는다. 앱에서 가장 눈에 띄는 종류의 오류다.
- 한국어 문자열 안에서 인용할 때는 `"..."` 대신 「」를 쓴다. 따옴표 이스케이프 사고를 원천 차단한다.
- 강조는 `<b>...</b>`, 낱말 강조는 `<span class='kw'>...</span>`. 그 외 태그는 쓰지 않는다.

## 검증

원고를 회차 객체 하나만 담은 파일로 저장한 뒤:

```bash
node scripts/check_day.js --lite day21.js
```

문법 오류, 대괄호·《》 짝, 필드 대응, `parts` 결합 일치, 개수 규칙, 퀴즈 정답 범위를 본다.
사람 눈으로는 잘 안 잡히는 것들이라, 통과할 때까지 돌리는 편이 훨씬 빠르다.
스크립트가 못 잡는 것 — 번역이 맞는지, 설명이 초보자에게 통하는지 — 은 심사 단계에서 잡는다.

## 음성에 대해

VOICEVOX로 미리 만든 음성이 문장별로 박혀 있다. 키는 **읽을 텍스트의 FNV-1a 32비트
해시(16진수 8자리)**다(계산법은 `references/schema-lite.md` 맨 아래).

새로 쓴 문장에는 그 음성이 없다. 이때 앱은 **브라우저 내장 음성으로 자동 대체한다.**
그러니 회차를 추가해도 앱은 깨지지 않고 소리도 난다 — 다만 목소리가 다르고 억양이 덜 자연스럽다.
사용자에게 이 점을 미리 한 줄로 알려라.

## 앱에 넣기

Claude 아티팩트로 게시되어 있고, 회차가 앱 파일 안에 박혀 있다(붙여넣기 화면이 없다).

**A. 원고만 전달 (기본값)** — 완성된 객체를 파일로 보내고, 사용자가 넣도록 한다.
넣는 자리는 `DATA` 배열의 맨 끝, 마지막 회차 객체 뒤에 쉼표를 찍고 이어 붙이는 것이다.

**B. 아티팩트를 직접 고치기** — 사용자가 요청하면 시도한다. 음성이 통째로 들어 있어
파일이 7MB를 넘는다. 아티팩트 읽기가 환경의 네트워크 정책에 막히는 일이 잦다.
막히면 `references/reading-the-app.md`의 우회 방법을 쓴다.
어느 쪽이든 **고치기 전에 원본을 백업**하고, 사용자에게 되돌릴 방법을 알려 둔다.

## 사용자에 대해

사용자는 본인을 "컴맹"이라고 말한다. 기술 용어를 앞세우지 말고, 무엇을 왜 하는지 짧은 문장으로 설명하고,
파일을 어디에 어떻게 넣는지는 단계별로 안내한다. 결과물은 항상 파일로 만들어 보낸다.
